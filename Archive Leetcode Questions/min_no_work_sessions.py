from sqlalchemy import between


class Solution:
    def minSessions(self, tasks: List[int], sessionTime: int) -> int:
        return None 
        


new_sol = Solution()

tasks = []
session_time = 10 

print(new_sol.minSessions(tasks, session_time))
